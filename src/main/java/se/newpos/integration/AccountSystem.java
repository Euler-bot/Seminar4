package se.newpos.integration;


import se.newpos.model.TransactionDTO;

/**
 * This is external accountsystem.
 */
public class AccountSystem {
    /**
     * Sends information of completed sale. Add when implementing in a real situation.
     * @param transactionDTO
     */
    public void emailTransactions(TransactionDTO transactionDTO){
    }
}
