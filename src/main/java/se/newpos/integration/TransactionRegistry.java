package se.newpos.integration;

import se.newpos.model.TransactionDTO;
/**
 * this is the db for transactions
 */
public class TransactionRegistry {
    
    public void saveTransaction(TransactionDTO transactionDTO){}
}
